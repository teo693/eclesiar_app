# Docker-optimized requirements
# Only modules actually used in Docker environment

# Podstawowe zależności
requests>=2.28.0
python-dotenv>=0.19.0

# Google Sheets API (używane w Docker)
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0

# Analiza danych (używane w raportach)
pandas>=1.5.0
numpy>=1.21.0

# Wizualizacja (używane w daily_report.py i html_report.py)
matplotlib>=3.5.0
seaborn>=0.11.0

# Eksport danych (używane w short_economic_report.py i daily_report.py)
python-docx>=0.8.11

# HTTP i API (requests już jest powyżej, ale dodajemy dla retry)
urllib3>=1.26.0
